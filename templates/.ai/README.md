# Deprecated Template Archive

`setup.sh` no longer copies this directory.

The active layout is `templates/AGENTS.md`, `templates/.agents/`, and `templates/.codex/`. This archive is retained temporarily as a source catalog; do not add new workflows, rules, or skills here.
